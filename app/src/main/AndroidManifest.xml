<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easyeventsfirebase">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logu_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.NavigationPrincipal"
            android:label="@string/title_activity_navigation_principal"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".Activity.InviInicio"
            android:parentActivityName=".Activity.Inicio" />
        <activity android:name=".Activity.Inicio" />
        <activity
            android:name=".Activity.EventoInvitado"
            android:parentActivityName=".Activity.OptionActivity" />
        <activity android:name=".Activity.OptionActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".Activity.EventoDetalle"
            android:parentActivityName=".Activity.EventoCreando" />
        <activity android:name=".Activity.RecuperarcontraActivity" />
        <activity android:name=".Activity.Privacidad" />
        <activity android:name=".Activity.Registrar" />
        <activity android:name=".Activity.Login" />
        <activity android:name=".Activity.EventoCreando" />
        <activity
            android:name=".Activity.CrearEvento"
            android:parentActivityName=".Activity.OptionActivity" />
        <activity android:name=".Activity.ActivityMaps" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>